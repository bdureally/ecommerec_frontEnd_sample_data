// they cannot be created inside a class
enum TextSizes{small,medium,large}

enum OrderStatus {processing,shipped,delivered}

enum PaymentMethod {paypal,googlePlay,applePay,creditCard,payStack,razorPay,paytm}